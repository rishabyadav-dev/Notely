import { atom } from "recoil";

export const notearrayState = atom({
  key: "notearrayState",
  default: [],
});

export const delnotearrayState = atom({
  key: "delnotearrayState",
  default: [],
});
